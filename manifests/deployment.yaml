apiVersion: apps/v1
kind: Deployment
metadata:
  name: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
        - name: my-container
          image: registry-intl.ap-southeast-3.aliyuncs.com/tinesh-poc-devops/java-webapp-docker:v1
          ports:
            - containerPort: 80
          env:
            - name: TAG
              value: v1